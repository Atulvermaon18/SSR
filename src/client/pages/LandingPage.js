import React from 'react';

const Landingpage = () => {
    return (
        <div>
            <h3>MMT Landing Screen</h3>
            <p>This is using Isomorphic, which means same kind of coding standard used both for Server and Client,
                In this case use are using React, which is using ES6 features, so all Node require will be changed to import.
            </p>
        </div>
    );
}

export default {
    component: Landingpage
}; 